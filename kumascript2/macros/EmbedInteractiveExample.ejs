<%
// Embeds a live sample from a interactive-examples.mdn.mozilla.net GitHub page
//
// Parameters:
//  $0 - The URL of interactive-examples.mdn.mozilla.net page (relative)
//  $1 - Optional custom height class to set on iframe element
//
//  Example call {{EmbedInteractiveExample("pages/css/animation.html", "taller")}}
//

let iClass = "interactive";
let url = kuma.url.resolve(env.interactive_examples.base_url, $0);
let siteURL = mdn.siteURL();
if (siteURL != 'https://developer.mozilla.org') {
    if (url.includes('?')) {
        url += '&origin=' + siteURL;
    } else {
        url += '?origin=' + siteURL;
    }
}

if ($0.includes('/js/')) {
    iClass += " interactive-js";
}

if ($1) {
    iClass += " " + $1;
}

%>
<iframe class="<%= iClass %>" width="100%" height="250" frameborder="0" src="<%= url %>" title="MDN Web Docs Interactive Example"></iframe>
