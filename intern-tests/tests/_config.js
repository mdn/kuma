define({

    // URLs
    homepageUrl: 'http://developer-local.allizom.org/',

    // Locales
    defaultLocale: 'en-US',

    // Important media queries
    mediaQueries: {
        smallDesktop: 1200,
        tablet: 1024,
        mobile: 768,
        smallMobile: 480
    },

    // Async testing in milliseconds
    testTimeout: 22000,

});
